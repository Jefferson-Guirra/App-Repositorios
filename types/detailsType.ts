export type Params = {
  id: string
}
export type Props = {
  data: string
}
export type TaskType = {
  id: string
  created: string | Date
  createdFormat?: string
  tarefa: string
  userId: string
  nome: string
}
